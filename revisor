#!/usr/bin/env bash
# Lanzador: ./revisor <comando> ...   (usa el venv del proyecto)
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PY="$DIR/.venv/bin/python"
[ -x "$PY" ] || PY=python3
exec "$PY" -m audit_agent.cli "$@"
